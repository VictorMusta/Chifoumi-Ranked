# Variables d'environnement — renfo-backend (template)
# Copier ce fichier en .env et compléter les valeurs

# PostgreSQL
DATABASE_URL=postgres://postgres:postgres@localhost:5432/renfo

# JWT
JWT_SECRET=change_me_in_production_use_a_long_random_string
JWT_EXPIRES_IN=7d
